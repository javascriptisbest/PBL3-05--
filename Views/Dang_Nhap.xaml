<Window x:Class="PBL3.Views.Dang_Nhap"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PBL3"
        xmlns:ViewModel="clr-namespace:PBL3.ViewModel"
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Title="Dang_Nhap" Height="382" Width="684">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="54"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Background="#72CDFF">
            <TextBlock Text="Đăng nhập" 
                     FontSize="24" 
                     FontWeight="Bold" 
                     VerticalAlignment="Center" 
                     Margin="54,0,0,0"
                     Foreground="	#2D2D5A"/>
            <materialDesign:PackIcon Kind="Home" Width="32" Height="32"  HorizontalAlignment="Left" Margin="15,8,0,0"/>
        </Grid>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.Background>
                <ImageBrush ImageSource="/Picture/background_DN.jpg" Opacity="0.4"/>
            </Grid.Background>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="136*"/>
                <ColumnDefinition Width="35*"/>

                <ColumnDefinition Width="171*"/>

            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Grid.ColumnSpan="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Circle with icons nằm trong cùng cột -->
                <Grid>
                    <Grid.Background>
                        <ImageBrush/>
                    </Grid.Background>
                    <Label Content="{Binding PhanQuyen}" 
                           FontSize="20"
                           FontWeight="ExtraBold"
                           VerticalAlignment="Top"
                           HorizontalAlignment="Center"
                           Margin="0,50,0,0"> </Label>
                    <!-- Circle -->
                    <Ellipse Width="120"
                             Height="120"
                             VerticalAlignment="Center"
                             HorizontalAlignment="Center"
                             Stroke="Black" 
                             StrokeThickness="0.5" Fill="White">
                    </Ellipse>
                    <Image Source="{Binding AnhHienTai}" 
                           Stretch="Uniform" 
                            Width="100" Height="100"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
                    <!-- Icon Left -->
                    <materialDesign:PackIcon Kind="ArrowLeftBoldCircleOutline"
                                  Width="35" Height="35"
                                  VerticalAlignment="Center"
                                  HorizontalAlignment="Left"
                                  Margin="40,0,0,0">
                    </materialDesign:PackIcon>
                    <!-- Tạo button bao quanh cái icon left để làm sự kiện-->
                    <Button  
                        Width="35" Height="35"  
                        VerticalAlignment="Center" 
                        HorizontalAlignment="Left"   
                        Margin="40,0,0,0" 
                        Background="Transparent"
                        BorderBrush="Transparent" Command="{Binding ButtonleftCommand}">
                    </Button>
                    <!-- Icon Right -->
                    <materialDesign:PackIcon Kind="ArrowRightBoldCircleOutline"
                                  Width="35" Height="35"
                                  VerticalAlignment="Center"
                                  HorizontalAlignment="Right"
                                  Margin="0,0,40,0"/>
                    <Button
                         Width="35" Height="35"  
                         VerticalAlignment="Center" 
                         HorizontalAlignment="Right"   
                         Margin="0,0,40,0"
                         Background="Transparent"
                         BorderBrush="Transparent" Command="{Binding ButtonrightCommand}" >
                        
                    </Button>
                    
                </Grid>
            </Grid>
            <!-- Login Form -->
            <Border Grid.Column="2" 
                    Background="White" 
                    CornerRadius="15" 
                    BorderThickness="1" 
                    BorderBrush="Black"
                    Margin="20,20,20,20"
             
                    >
                <StackPanel Margin="16">
                    <!-- Email -->
                    <StackPanel >

                        <TextBox materialDesign:HintAssist.Hint="Email"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 FontSize="14"
                                 FontWeight="Bold"
                                 Margin="10,20,10,0"
                                 Text="{Binding Email}"                                    
                                 />
                    </StackPanel>

                    <!-- Password -->
                    <StackPanel>

                        <PasswordBox materialDesign:HintAssist.Hint="Password "
                                    Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                                    FontSize="14"
                                    FontWeight="Bold"
                                    Margin="10,20,10,0"/>
                    </StackPanel>

                    <!-- Login Button -->
                    <Button Content="Đăng nhập"
                            Style="{StaticResource MaterialDesignRaisedLightButton}"
                            HorizontalAlignment="Center"
                            Margin="0,35,0,10"
                           Background="#C4EEFF"
                            Width="99"
                            Height="36" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
